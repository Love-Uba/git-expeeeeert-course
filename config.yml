title: GIT EXPEEEEERT
description: My First Git Tests
template:
  repo: git-expeeeeert-template
  name: git-expeeeeert
before:
  - type: createIssue
    title: Welcome!
    body: new-issue.md
steps:
  - title: First step
    description: The first step in your course
    event: issues.assigned
    link: '{{ repoUrl }}'
    actions:
      - type: closeIssue
